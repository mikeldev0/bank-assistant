# AIFindr: server-side only. Never NEXT_PUBLIC_*.
AIFINDR_API_KEY=
AIFINDR_PROJECT_ID=
AIFINDR_CLIENT_ID=
AIFINDR_ORGANIZATION_ID=
# Configure API URL and payload only after verifying the provided Private API spec.
